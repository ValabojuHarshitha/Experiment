<h1>Listing ads</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Contact</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ads.each do |ad| %>
  <tr>
    <td><%= ad.name %></td>
    <td><%= ad.contact %></td>
    <td><%= link_to 'Show', ad %></td>
    <td><%= link_to 'Edit', edit_magazine_ad_path(ad) %></td>
    <td><%= link_to 'Destroy', ad, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ad', new_magazine_ad_path %>
